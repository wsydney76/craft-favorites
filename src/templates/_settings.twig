{# @var plugin \wsydney76\favorites\Plugin #}
{# @var settings \wsydney76\favorites\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.colorField({
    label: 'Star Color',
    name: 'starColor',
    value: settings.starColor,
    instructions: 'The color used for the star icon',
    tip: 'Default: #ca8a04',
    required: true,
    errors: settings.errors('starColor')
}) }}

{{ forms.lightswitchField({
    label: 'Load Assets',
    name: 'loadAssets',
    on: settings.loadAssets,
    instructions: 'Whether the plugin should load its own JavaScript and CSS assets.',
    warning: 'If set to off, your project must include the assets into its own asset bundle.<br> And keep them up to date.'
}) }}
